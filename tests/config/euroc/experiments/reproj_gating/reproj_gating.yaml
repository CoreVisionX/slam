# @package _global_

defaults:
  - /euroc/vio
  - _self_

ba:
  config:
    reprojection_gating: true
    use_light_relative_pose_factor: false
    light_relative_pose_sigmas: [0.2, 0.2, 0.2, 0.5, 0.5, 0.5]
